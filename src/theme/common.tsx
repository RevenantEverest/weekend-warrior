import { CommonThemeColors } from '../types/theme';

const colors: CommonThemeColors = {
    primary: "#c6be05",
    secondary: "#c76304",
    mdb: {
        primary: ""
    }
};

const classNames = {

};

const common = {
    colors,
    classNames
};

export default common;